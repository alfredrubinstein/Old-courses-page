//esta interfaz es para definir el tipo de dato que se va a recibir en el body de la petición
export interface User {
    id: number;
    name: string;
    email: string;
    password: string;
  }
  